# Session 10 février 2026 — Organisation projet & insights

## Ce qui a été fait

### 1. Analyse du rapport /insights
- Revue du rapport d'utilisation Claude Code (47 sessions analysées)
- Identification des points de friction récurrents : Claude qui code quand on demande d'analyser, mauvaises approches de debug, pertes de contexte inter-sessions

### 2. Mise à jour de CLAUDE.md — Section Core Rules
Ajout de 6 règles issues des suggestions du rapport :
- Ne pas coder quand on demande de planifier/analyser/documenter
- Vérifier la couche (TS/Rust) avant d'éditer
- Investiguer la cause racine avant de proposer des workarounds
- Valider les fichiers de config après modification (cargo build, jq, bash -n)
- Vérifier la racine git avant les commandes git
- Sauvegarder les résumés de session dans `docs/`

### 3. Création du skill /context
- Créé `.claude/skills/context/SKILL.md`
- En début de session, `/context` lit tous les fichiers `docs/*.md` + git log récent et produit un résumé de l'état du projet
- Créé le répertoire `docs/`

### 4. Réorganisation des fichiers de documentation
- Déplacé `session_2026-02-06.md` → `docs/`
- Déplacé `session_2026-02-09.md` → `docs/`
- Déplacé `todo_clear_engine.md` → `docs/`

### 5. Vérification du todo nettoyage moteurs
- **Phases 1-5 : toutes DONE** (suppression ONNX Runtime, déduplication transcribe, déplacement TranscriptionLanguage, suppression modules inutiles, hygiène chemins hardcodés)
- **Section "Autres améliorations" : 11 items restants** (5 backend Rust + 6 frontend React, principalement de la déduplication de code)
- Mis à jour `docs/todo_clear_engine.md` avec les phases cochées

## État actuel du projet

- **Moteurs actifs** : OpenVINO + CoreML uniquement (ONNX Runtime supprimé)
- **CLAUDE.md** : enrichi avec les Core Rules
- **Skill /context** : opérationnel pour le chargement de contexte inter-sessions
- **Documentation** : centralisée dans `docs/`

## Problèmes connus

Aucun nouveau problème identifié cette session.

## Prochaines étapes possibles

- Refactoring backend Rust : `prepare_audio()`, `get_transcription_by_id()`, unifier RMS, `format_duration()`/`format_timestamp()`, mapping DB
- Refactoring frontend React : `formatters.ts`, `getDecodingConfig()`, composants partagés (`CopyButton`, `PanelHeader`, `RangeSlider`), `@layer components` Tailwind
- Voir `docs/todo_clear_engine.md` pour la liste complète
